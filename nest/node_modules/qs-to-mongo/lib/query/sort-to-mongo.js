"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sortToMongo = void 0;
function sortToMongo(sort) {
    return sort
        ? sort.split(',').reduce((hash, field) => {
            let sign = 1;
            if (field.startsWith('-')) {
                field = field.substr(1);
                sign = -1;
            }
            hash[field.trim()] = sign;
            return hash;
        }, {})
        : null;
}
exports.sortToMongo = sortToMongo;
//# sourceMappingURL=sort-to-mongo.js.map